name: "FTP Sync (ETML)"
description: "Synchronise un répertoire local vers le serveur FTP ETML via SamKirkland/FTP-Deploy-Action"
author: "Section INF"

inputs:
  local-dir:
    description: "Répertoire local à synchroniser"
    required: true

  custom-dir:
    description: "Répertoire distant (FTP), ex: cours"
    required: true

  ictroot-dir:
    description: "Répertoire root des modules ICT, ex: moduleICT"
    required: true  

  ictmodule-dir:
    description: "Le répertoire du module ICT, ex: 346"  
    required: true  

  state-name:
    description: "Nom du fichier d'état pour le delta FTP"
    required: false
    default: ".ftp-sync-state.json"

  log-level:
    description: "Niveau de logs (minimal/basic/standard/verbose)"
    required: false
    default: "minimal"

  timeout:
    description: "Timeout en ms"
    required: false
    default: "300000"

  ftp-server:
    description: "Nom du serveur FTP (ex: enseignement.section-inf.ch)"
    required: true

  ftp-username:
    description: "Nom d'utilisateur FTP"
    required: true

  ftp-password:
    description: "Mot de passe FTP"
    required: true

runs:
  using: "composite"
  steps:
    - name: 🔒 Vérification des secrets requis
      env:
        FTP_SERVER: ${{ inputs.ftp-server }}
        FTP_USERNAME: ${{ inputs.ftp-username }}
        FTP_PASSWORD: ${{ inputs.ftp-password }}
      shell: bash
      run: |
        set -euo pipefail

        missing=0

        if [[ -z "${FTP_SERVER:-}" ]]; then
          echo "❌ ERREUR : La variable FTP_SERVER est vide ou absente."
          missing=1
        fi

        if [[ -z "${FTP_USERNAME:-}" ]]; then
          echo "❌ ERREUR : La variable FTP_USERNAME est vide ou absente."
          missing=1
        fi

        if [[ -z "${FTP_PASSWORD:-}" ]]; then
          echo "❌ ERREUR : La variable FTP_PASSWORD est vide ou absente."
          missing=1
        fi

        if [[ "$missing" -eq 1 ]]; then
          echo "⛔ Arrêt : Les secrets FTP ne sont pas transmis correctement depuis le workflow appelant."
          exit 1
        fi

        echo "✅ Secrets FTP détectés."

    - name: 📝 Créer placeholder state.json si nécessaire (et arborescence)
      shell: bash
      env:
        FTP_SERVER:   ${{ inputs.ftp-server }}
        FTP_USERNAME: ${{ inputs.ftp-username }}
        FTP_PASSWORD: ${{ inputs.ftp-password }}

        ICTROOT:      ${{ inputs.ictroot-dir }}
        ICTMODULE:    ${{ inputs.ictmodule-dir }}
        CUSTOMDIR:    ${{ inputs.custom-dir }}
        STATE_NAME:   ${{ inputs.state-name }}
      run: |
        set -euo pipefail

        STATE_URL="ftp://${FTP_SERVER}/${ICTROOT}/${ICTMODULE}/${CUSTOMDIR}/${STATE_NAME}"

        echo "🔍 Vérification du fichier state : ${STATE_URL}"

        # 1) HEAD sur le fichier distant (échoue si dossier ou fichier manquant)
        if ! curl -k --ssl-reqd --ftp-pasv \
          --user "${FTP_USERNAME}:${FTP_PASSWORD}" \
          --head "${STATE_URL}" >/dev/null 2>&1; then

          echo "📝 Fichier de state absent, création d'un placeholder vide..."
          echo "{}" > "${STATE_NAME}"

          # 2) Upload avec création automatique de toute l'arborescence
          curl -k --ssl-reqd --ftp-pasv --ftp-create-dirs \
            --user "${FTP_USERNAME}:${FTP_PASSWORD}" \
            --upload-file "${STATE_NAME}" \
            "${STATE_URL}"
        else
          echo "✅ Fichier de state déjà présent."
        fi


    - name: 📂 Sync via FTP-Deploy-Action
      uses: SamKirkland/FTP-Deploy-Action@v4.3.6
      with:
        server:     ${{ inputs.ftp-server }}
        username:   ${{ inputs.ftp-username }}
        password:   ${{ inputs.ftp-password }}
        protocol:   ftps
        port:       21
        local-dir:  ${{ inputs.local-dir }}
        server-dir: ${{ inputs.ictroot-dir }}/${{ inputs.ictmodule-dir}}/${{ inputs.custom-dir }}/
        state-name: ${{ inputs.state-name }}
        log-level:  ${{ inputs.log-level }}
        timeout:    ${{ inputs.timeout }}
